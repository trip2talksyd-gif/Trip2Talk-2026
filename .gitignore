# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr

# Env / secrets — never commit
.env
.env.local
.env*.local

# Staff PIN seed (placeholder or real PINs) — run manually in SQL Editor only
supabase/seed/staff_profiles_seed.sql

# One-off local ops (lockout PIN reset, etc.) — never commit
scripts/one-off/

# Local scratch (SQL probes, image reprocess) — never commit
.temp/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

.vercel
.cursor/
dist_old_*/
tmp_media/
tmp_*.png
tmp_*.webp
tmp_*.zip
tmp_*.sql
tmp_*.mjs
